version: 0.2

# ─────────────────────────────────────────────────────────────
# Set these variables in CodeBuild Environment Variables:
#
#   AWS_DEFAULT_REGION        = ap-south-1
#   AWS_ACCOUNT_ID            = 123456789012
#   IMAGE_REPO_NAME           = predictpod-backend
#   FRONTEND_IMAGE_REPO_NAME  = predictpod-frontend
#   REACT_APP_BACKEND_URL     = https://your-api-url
# ─────────────────────────────────────────────────────────────

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - BACKEND_REPO_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME
      - FRONTEND_REPO_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$FRONTEND_IMAGE_REPO_NAME
      - IMAGE_TAG=latest
      - echo Backend URI is $BACKEND_REPO_URI
      - echo Frontend URI is $FRONTEND_REPO_URI

  build:
    commands:
      - echo Build started on `date`

      - echo Building the Backend Docker image...
      - docker build -t $BACKEND_REPO_URI:$IMAGE_TAG -f backend/Dockerfile backend/

      - echo Building the Frontend Docker image...
      - docker build -t $FRONTEND_REPO_URI:$IMAGE_TAG -f frontend/Dockerfile --build-arg REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL frontend/

  post_build:
    commands:
      - echo Build completed on `date`

      - echo Pushing Backend image to ECR...
      - docker push $BACKEND_REPO_URI:$IMAGE_TAG

      - echo Pushing Frontend image to ECR...
      - docker push $FRONTEND_REPO_URI:$IMAGE_TAG

      - echo Writing imagedefinitions.json...
      - printf '[{"name":"pp3container","imageUri":"%s"},{"name":"frontend","imageUri":"%s"}]' $BACKEND_REPO_URI:$IMAGE_TAG $FRONTEND_REPO_URI:$IMAGE_TAG > imagedefinitions.json
      - echo imagedefinitions.json contents:
      - cat imagedefinitions.json

artifacts:
  files:
    - imagedefinitions.json
